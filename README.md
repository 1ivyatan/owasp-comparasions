# owasp-comparasions

Demonstration of web security practices and solutions in frameworks - plain PHP and Laravel.


## Setup

Required software: composer, npm.

* `composer install`
* `npm install`
* `npm run build`
* `php artisan migrate`
* `php artisan key:generate`
* `composer run dev`